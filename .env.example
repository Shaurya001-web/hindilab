# Google Gemini API Key (optional — mock mode works without it)
GEMINI_API_KEY=

# Azure Speech Service (optional — mock mode works without it)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# Set to "true" to use mock data instead of real APIs
MOCK_MODE=true
